# TOKI static HTML prototype

Open the `index.html`, `login.html`, or `register.html` files in a browser to view the pages. No build step is required.

Quick test (Linux):

```bash
# from project root
xdg-open index.html
```
